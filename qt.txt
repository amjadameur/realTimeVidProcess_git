
button->setGeometry(x, y, buttonWidth, buttonHeight);

The constructor of PlayerInterface, calls the constructor of QWidget, it initializes a window,
then the method setLayout of QWidget sets the constracted layout as the layout manager of the window.

QHBoxLayout : lines up widgets horizontally
QVBoxLayout : lines up widgets vertically

signal : is a message that an object can send, most of the time to inform of a status change.
slot   	: is a function that is used to accept and respond to a signal.